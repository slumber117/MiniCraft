package minicraft.entity.animals;

import minicraft.entity.*;

/** Dog – passive but will assist the player against nearby predators (future feature). */
public class Dog extends PassiveAnimal {
    public Dog() { super(EntityType.DOG); }
}
